# BSV 架构与构建说明

## 模块职责

- **src/controller**: 对外控制器接口与实现，唯一对外导出的接口在 include/bsv/controller.h（依赖 include/bsv/core_types.h 与 include/bsv/buffer.h）。
- **src/camera**: 摄像头模块，包含 include 与平台实现目录。
- **src/csc**: 色彩空间转换模块，包含 include 与平台实现目录。
- **tests/linux**: Linux 场景与单元测试。
- **tests/android**: Android 平台测试占位。

## 构建方式

### 使用 CMakePresets (推荐)

- Linux:
  ```bash
  cmake --preset linux
  cmake --build --preset linux
  ```
- Android (NDK r25c):
  ```bash
  cmake --preset android
  cmake --build --preset android
  ```

### 传统 CMake 命令

- Linux:
  ```bash
  cmake -S . -B build/linux -DCMAKE_BUILD_TYPE=Debug -DBSV_BUILD_TESTS=ON -DBSV_PLATFORM=linux
  cmake --build build/linux
  ```
- Android:
  ```bash
  cmake -S . -B build/android \
    -DCMAKE_BUILD_TYPE=Debug \
    -DBSV_BUILD_TESTS=OFF \
    -DBSV_PLATFORM=android \
    -DCMAKE_TOOLCHAIN_FILE=/opt/android-ndk-r25c/build/cmake/android.toolchain.cmake \
    -DANDROID_NDK=/opt/android-ndk-r25c \
    -DANDROID_ABI=arm64-v8a \
    -DANDROID_PLATFORM=android-24
  cmake --build build/android
  ```

## 平台差异

- **缓冲类型**:
  - Linux 侧使用主机内存与 EGL/GLES 进行 GPU CSC。
  - Android 侧使用 AHardwareBuffer 作为平台句柄，并通过 EGLImage 进行 GPU 渲染。
- **图形上下文**:
  - Linux 使用 EGL PBuffer 或 surfaceless display 初始化 GLES。
  - Android 通过 NDK EGL 与 GLES2/3 初始化，优先尝试 GLES3。
- **摄像头实现**:
  - Android 使用 Camera2 + AImageReader。
  - Linux 目前为抽象接口，具体实现由上层集成。
